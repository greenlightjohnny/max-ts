import React from "react"
import axios from "axios"

type SubmitFormI = {
  title: string
  body: string
}

const SubmitForm = (props: SubmitFormI) => {
  console.log(props)
  return <div></div>
}

export default SubmitFormI
